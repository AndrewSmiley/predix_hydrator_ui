
import { ElementFinder } from 'protractor';

import { PourSelectComponent } from './pour-select.component';

describe('PourSelect: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
           // this.instance = new PourSelectComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
