
import { ElementFinder } from 'protractor';

import { SplashComponent } from './splash.component';

describe('splash: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            this.instance = new SplashComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
