import { Component, ViewEncapsulation } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'splash',
    templateUrl: './splash.html',
    styleUrls: ['./splash.less'],
    encapsulation: ViewEncapsulation.None
})

export class SplashComponent {
    
    constructor() {}
}
