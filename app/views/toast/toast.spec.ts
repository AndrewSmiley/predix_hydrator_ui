
import { ElementFinder } from 'protractor';

import { toastComponent } from './toast.component';

describe('toast: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            //this.instance = new toastComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
