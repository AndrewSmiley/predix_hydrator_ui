
import { ElementFinder } from 'protractor';

import { StatusComponent } from './status.component';

describe('Status: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            //this.instance = new StatusComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
