
import { ElementFinder } from 'protractor';

import { CreditsComponent } from './credits.component';

describe('credits: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
            this.instance = new CreditsComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
