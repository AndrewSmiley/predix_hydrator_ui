import { Component, ViewEncapsulation } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'credits',
    templateUrl: './credits.html',
    styleUrls: ['./credits.less'],
    encapsulation: ViewEncapsulation.None
})

export class CreditsComponent {
    
    constructor() {}
}
