
import { ElementFinder } from 'protractor';

import { PourProgressComponent } from './pour-progress.component';

describe('PourProgress: ', () => {
    describe('Events and Lifecycle', () => {
        beforeEach(() => {
           // this.instance = new PourProgressComponent();
        });

        it('Properties are defined', () => {
            // TODO
        });
    });
});
